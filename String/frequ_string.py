# text = "apple"

# freq = {}

# for ch in text:
#     if ch in freq:
#         freq[ch] = freq[ch] + 1
#     else:
#         freq[ch] = 1

# for key in freq:
#     print(key, ":", freq[key])













string = "banana"

freq = { }

for ch in string :
    if ch in freq :
        freq[ch] = freq[ch] + 1 
        
    else : 
        freq[ch] = 1 
        
for key in freq :
    print(key , freq[key])





         